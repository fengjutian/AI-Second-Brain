# AI 第二大脑 — 需求文档

## 1. 产品定位

一个**本地优先、AI 增强**的个人知识管理系统。像 Obsidian 一样管理 Markdown 笔记，同时让 AI 成为你的知识伙伴——自动整理、智能检索、辅助创作。

核心信条：**你知即 AI 所知，AI 所知皆可为你所用。**

---

## 2. 核心功能需求

### 2.1 笔记编辑

| 编号 | 需求 | 优先级 | 说明 |
|------|------|--------|------|
| N1 | Markdown 实时预览 | P0 | 分屏/切换编辑与预览，支持 GitHub Flavored Markdown |
| N2 | 所见即所得 (WYSIWYG) | P2 | 可选，类似 Typora 的行内渲染 |
| N3 | 代码块高亮 | P0 | 支持主流语言语法高亮 |
| N4 | 图片粘贴/拖拽 | P1 | 粘贴板图片自动保存到本地，拖拽插入 |
| N5 | 表格编辑 | P1 | Markdown 表格可视化编辑 |
| N6 | 数学公式 | P2 | LaTeX / MathJax 支持 |
| N7 | Callout / 提示块 | P2 | 类似 Obsidian 的 `> [!note]` 语法 |
| N8 | 标签支持 | P0 | `#tag` 行内标签 + Frontmatter `tags:` |
| N9 | 属性 / Frontmatter | P0 | YAML frontmatter，可编辑元数据 |

### 2.2 知识链接

| 编号 | 需求 | 优先级 | 说明 |
|------|------|--------|------|
| L1 | 双向链接 | P0 | `[[笔记名]]` 语法，自动生成反向链接面板 |
| L2 | 自动补全 | P0 | 输入 `[[` 时弹出笔记搜索补全 |
| L3 | 悬停预览 | P1 | 鼠标悬停链接时弹出笔记内容预览 |
| L4 | 未链接提及 | P2 | 显示提到了某笔记名但未加 `[[]]` 的位置 |
| L5 | 链接图谱 | P0 | 节点-边图展示笔记关联，可交互缩放/拖拽 |
| L6 | 局部图谱 | P1 | 仅展示当前笔记的一跳/二跳邻居 |
| L7 | 出链/反链面板 | P0 | 侧栏显示当前笔记的所有出链和反链 |

### 2.3 知识组织

| 编号 | 需求 | 优先级 | 说明 |
|------|------|--------|------|
| O1 | 文件浏览器 | P0 | 树形目录结构，创建/删除/重命名/移动 |
| O2 | 多仓库 (Vault) | P0 | 支持打开/切换不同知识库 |
| O3 | 书签 / 星标 | P1 | 标记常用笔记快速访问 |
| O4 | 笔记别名 | P2 | 为笔记设置别名用于链接 |
| O5 | 模板 | P2 | 新建笔记时可选择模板 |
| O6 | 日记 (Daily Note) | P1 | 一键创建/打开当天日记 |
| O7 | Canvas / 白板 | P3 | 无限画布自由排布笔记卡片 |

### 2.4 搜索

| 编号 | 需求 | 优先级 | 说明 |
|------|------|--------|------|
| S1 | 全文搜索 | P0 | 快速全文检索，高亮命中 |
| S2 | 模糊搜索 | P1 | 容错拼写，拼音搜索 |
| S3 | 高级筛选 | P1 | 按标签/路径/日期/属性过滤 |
| S4 | 搜索历史/保存 | P3 | 保存常用搜索条件 |

### 2.5 AI 能力（差异化核心）

| 编号 | 需求 | 优先级 | 说明 |
|------|------|--------|------|
| A1 | 语义搜索 | P0 | 用自然语言搜笔记，按语义相关度排序 |
| A2 | AI 聊天 (RAG) | P0 | 基于整个知识库的问答，引用来源笔记 |
| A3 | 自动标签 | P1 | AI 为新笔记自动建议标签 |
| A4 | 智能摘要 | P1 | 为一篇或一组笔记自动生成摘要 |
| A5 | 写作辅助 | P1 | 续写、改写、翻译、润色选中文本 |
| A6 | 关键词提取 | P2 | 从笔记中提取关键概念 |
| A7 | 知识发现 | P2 | 自动发现未被链接但内容相关的笔记 |
| A8 | 自然语言创建笔记 | P2 | 用自然语言描述，AI 生成结构化笔记 |
| A9 | 定期回顾 | P3 | AI 根据遗忘曲线推荐需要复习的笔记 |
| A10 | 本地模型支持 | P1 | 支持本地 LLM (Ollama 等)，隐私优先 |

### 2.6 数据管理

| 编号 | 需求 | 优先级 | 说明 |
|------|------|--------|------|
| D1 | 本地文件存储 | P0 | 所有笔记为纯 .md 文件，无数据库锁定 |
| D2 | 附件管理 | P0 | 图片等附件统一放在子目录 |
| D3 | 自动保存 | P0 | 编辑后自动保存 |
| D4 | 版本历史 | P2 | 本地 Git 集成或内置版本记录 |
| D5 | 导入 | P1 | 导入 Obsidian/Notion/Markdown 文件 |
| D6 | 导出 | P2 | 导出为 PDF / HTML / 纯 Markdown 合集 |
| D7 | 备份 | P1 | 一键备份整个知识库 |

### 2.7 同步与协作

| 编号 | 需求 | 优先级 | 说明 |
|------|------|--------|------|
| Y1 | 多设备同步 | P2 | 通过本地文件夹/云盘/Git 同步 |
| Y2 | 端到端加密 | P3 | 敏感笔记加密存储 |
| Y3 | 发布 | P3 | 将选中笔记发布为网页 |

### 2.8 用户体验

| 编号 | 需求 | 优先级 | 说明 |
|------|------|--------|------|
| U1 | 多标签页 | P0 | 同时打开多篇笔记 |
| U2 | 分屏 | P1 | 左右/上下分屏编辑 |
| U3 | 命令面板 | P0 | `Ctrl+P` 快速命令搜索与执行 |
| U4 | 快捷键体系 | P0 | 常用操作可自定义快捷键 |
| U5 | 深色/浅色主题 | P0 | 系统主题跟随 + 手动切换 |
| U6 | 自定义 CSS | P2 | 用户自定义样式 |
| U7 | 国际化 | P2 | 中英文界面切换 |
| U8 | 移动端适配 | P3 | 响应式布局或独立移动端 |
| U9 | 启动速度 | P0 | 冷启动 < 2s，搜索响应 < 100ms |

### 2.9 插件系统

| 编号 | 需求 | 优先级 | 说明 |
|------|------|--------|------|
| P1 | 插件 API | P2 | 开放 API，社区可开发插件 |
| P2 | 插件市场 | P3 | 浏览/安装/管理社区插件 |
| P3 | 内置核心插件 | P1 | 核心功能以插件形式实现，保证架构可扩展 |

---

## 3. 非功能需求

### 3.1 技术原则

- **本地优先 (Local-first)**：所有数据存于本地文件系统，无强制云服务
- **隐私至上**：AI 功能默认支持本地模型，不上传用户笔记
- **文件即数据**：纯 Markdown 文件，任何文本编辑器均可读写
- **可扩展**：插件架构，核心功能自身也是插件
- **跨平台**：Windows / macOS / Linux 桌面端优先

### 3.2 性能

- 万级笔记量下搜索 < 200ms
- 图谱渲染 5000 节点不卡顿
- 编辑器大文件 (10万行) 不卡顿

### 3.3 安全

- 不自动联网上传任何笔记内容
- AI 功能联网需用户显式授权
- 第三方插件需权限声明

---

## 4. 版本规划建议

### MVP (v0.1) — 可用原型
- Markdown 编辑 + 预览
- 文件浏览器 + 多标签页
- `[[双向链接]]` + 自动补全
- 全文搜索
- 基础图谱
- 命令面板
- 深色/浅色主题

### v0.2 — AI 初步接入
- 语义搜索
- AI 聊天 (RAG)
- 自动标签建议

### v0.3 — 体验完善
- 日记功能
- 反链面板
- 悬停预览
- 导入 Obsidian 仓库

### v1.0 — 正式版
- 插件系统
- 完整 AI 能力
- 性能优化
- 国际化

---

## 5. 技术选型

| 层面 | 选型 | 说明 |
|------|------|------|
| 桌面框架 | **Tauri** (Rust) | 轻量、高性能、跨平台 |
| 前端 UI | **React** + TypeScript | 生态成熟，组件丰富 |
| 编辑器内核 | **TipTap** | 基于 ProseMirror，WYSIWYG 友好，插件生态丰富 |
| 图谱渲染 | **Cytoscape.js** | 图论级渲染，支持布局算法和交互 |
| 后端 | **FastAPI** (Python) | AI/RAG 生态无缝对接 |
| AI 嵌入 | sentence-transformers / OpenAI | 支持本地 + 云端 |
| AI 对话 | **LlamaIndex** | RAG 框架，文档解析 + 索引 + 检索一体化 |
| 向量数据库 | **ChromaDB** | 轻量、本地优先、Python 原生 |
| 搜索索引 | **SQLite FTS5** | 后端全文索引，与 ChromaDB 共用一个 SQLite 实例，零额外依赖 |
| 状态管理 | **Zustand** | 轻量、TS 友好、中间件齐全 |
| CSS 方案 | **Tailwind CSS** | 原子化 CSS，快速、一致性强 |
| 组件库 | **Radix UI** (Headless) | 无样式组件，配合 Tailwind 自定义风格 |

### 工程决策

| 决策项 | 结论 |
|--------|------|
| IPC 通信 | Tauri sidecar 自动拉起 FastAPI，前端通过 `localhost:{port}` HTTP/WebSocket 通信 |
| 后端部署 | 开发：手动 `uvicorn --reload`；生产：PyInstaller 打包，Tauri sidecar 捆绑 |
| 文件监听 | FastAPI 后端 `watchdog` 监听 vault 目录，变更后重建 FTS5 索引 + 向量嵌入，通过 WebSocket 推送前端刷新 |
| 前端路由 | React Router（页面级导航）+ 自建 TabManager 组件（多标签页状态） |

### 架构概览

```
┌──────────────────────────────────┐
│        Tauri Desktop App         │
│  ┌────────────────────────────┐  │
│  │  React + TypeScript        │  │
│  │  ├─ Tailwind CSS + Radix   │  │
│  │  ├─ Zustand (state)        │  │
│  │  ├─ TipTap (editor)        │  │
│  │  ├─ Cytoscape.js (graph)   │  │
│  │  └─ TabManager + Router    │  │
│  └──────────────┬─────────────┘  │
│                 │ HTTP + WS       │
│  ┌──────────────▼─────────────┐  │
│  │  FastAPI (sidecar)         │  │
│  │  ├─ REST API                │  │
│  │  ├─ WebSocket (file watch)  │  │
│  │  ├─ LlamaIndex RAG Engine   │  │
│  │  ├─ SQLite FTS5 (search)    │  │
│  │  └─ ChromaDB (vectors)      │  │
│  └──────────────┬─────────────┘  │
│                 │                 │
│  ┌──────────────▼─────────────┐  │
│  │  Local Storage             │  │
│  │  ├─ *.md (notes)           │  │
│  │  ├─ attachments/           │  │
│  │  └─ data/ (SQLite + Chroma)│  │
│  └────────────────────────────┘  │
└──────────────────────────────────┘
```

---

## 6. 本地优先架构策略

### 6.1 核心原则

系统必须遵循 **本地优先 (Local-first)** 原则：

1. **无后端可运行**：在 Tauri 桌面端，即使 FastAPI 后端未启动/不可用，核心笔记功能（浏览、编辑、创建、删除）必须可用
2. **降级而非崩溃**：需要后端的功能（AI、搜索、图谱等）在后端不可用时应优雅降级，展示明确提示，不影响其他功能
3. **浏览器模式依赖后端**：在纯浏览器开发模式下，后端是必需的（HTTP API 是唯一数据通道）
4. **文件即真实**：所有数据以 `.md` 文件存储于本地文件系统，不以来数据库

### 6.2 功能降级矩阵

| 功能 | 模式 | 后端依赖 | 降级状态 | 降级行为 |
|------|------|----------|----------|----------|
| 📂 文件列表 | Tauri | ✅ 已降级 | `@tauri-apps/plugin-fs` `readDir()` | 直读文件系统，零后端依赖 |
| | 浏览器 | ❌ 必须 | 无降级 | 依赖 `api.notes.list()` |
| 📖 打开笔记 | Tauri | ✅ 已降级 | `readTextFile()` | 直读 .md 文件，自动剥离 frontmatter |
| | 浏览器 | ❌ 必须 | 无降级 | 依赖 `api.notes.get()` |
| ✏️ 自动保存 | Tauri | ❌ **未降级** | ⚠️ 高风险 | 数据丢失风险——编辑后若后端不可用，内容存在内存中，刷新即丢失 |
| | 浏览器 | ❌ 必须 | 无降级 | 依赖 `api.notes.update()` |
| ➕ 新建笔记 | Tauri | ❌ **未降级** | ⚠️ 高风险 | 需 Tauri FS `writeTextFile()` + frontmatter 生成 |
| | 浏览器 | ❌ 必须 | 无降级 | 依赖 `api.notes.create()` |
| 🗑️ 删除笔记 | Tauri | ✅ 已降级 | `remove()` 作为 fallback | 先试 API，失败则直删 |
| | 浏览器 | ❌ 必须 | 无降级 | 依赖 `api.notes.delete()` |
| 📅 日记 (Daily) | 两种 | ❌ **未降级** | 中风险 | 依赖 `api.daily.today()` |
| 🔗 出链/反链 | 两种 | ❌ **未降级** | 中风险 | 依赖 `api.notes.outgoingLinks/backlinks()` |
| 🔍 全文搜索 | 两种 | ❌ **未降级** | 中风险 | 依赖 `api.search.keyword()` |
| 🕸️ 知识图谱 | 两种 | ❌ **未降级** | 低风险 | 显示"无法加载"提示 |
| 🤖 AI 聊天 | 两种 | ❌ **未降级** | 低风险 | 显示"AI 服务不可用" |
| 🧠 AI 配置 | 两种 | ❌ **未降级** | 低风险 | 设置页永久加载中 |
| 📥 导入 Obsidian | 两种 | ❌ **未降级** | 低风险 | 偶尔使用 |
| 🎨 白板 (tldraw/Excalidraw) | 两种 | ✅ 零依赖 | ✅ 完美 | 纯前端，无后端需求 |
| 🌓 主题切换 | 两种 | ✅ 零依赖 | ✅ 完美 | localStorage |
| 🔌 核心插件 | 两种 | ✅ 零依赖 | ✅ 完美 | 纯前端注册 |

### 6.3 待实现的降级方案（优先级排序）

| 优先级 | 功能 | 当前状态 | 建议实现 |
|--------|------|----------|----------|
| **P0** | 新建笔记 | ❌ 无降级 | Tauri: 用 `writeTextFile()` 写 .md 文件（含 YAML frontmatter），同时通知后端异步索引。浏览器: 无降级（必须后端） |
| **P0** | 自动保存 | ❌ 无降级 | Tauri: 用 `writeTextFile()` 直接覆写 .md 文件。前端维护内容缓存，后端可用时同步索引 |
| **P1** | 日记 | ❌ 无降级 | Tauri: 前端按日期计算路径（`daily/YYYY-MM-DD.md`），`exists()` 检查，不存在则用模板创建 |
| **P1** | 搜索 | ❌ 无降级 | Tauri: 前端正则全文搜索（小规模可用）。大规模依赖后端 FTS5 |
| **P1** | 出链/反链 | ❌ 无降级 | Tauri: 前端解析所有笔记的 `[[link]]` 构建内存索引 |
| **P2** | 图谱 | ❌ 无降级 | 显示"后端未运行"提示即可，非核心路径 |
| **P2** | Vault 操作 | ❌ 无降级 | Tauri: `settingsStore` 直接记录 vaultPath 到 localStorage，跳过 `api.vaults.open()` |

### 6.4 架构约束

- **Tauri mode**: `isTauri()` === true → 所有文件 I/O 优先使用 `@tauri-apps/plugin-fs`，后端 API 作为增强（索引、AI）
- **Browser mode**: `isTauri()` === false → 所有操作必须通过 HTTP API，无法降级
- **前端数据层**: `useNoteStore` 为纯内存缓存，不依赖后端。由调用方（FileTree、Editor 等）负责填充数据
- **文件格式**: 每个 `.md` 文件包含 YAML frontmatter（id、title、tags、created、updated），内容为纯 Markdown

### 6.5 环境检测

```ts
// src/lib/env.ts
export const isTauri = (): boolean =>
  !!(window as any).__TAURI_INTERNALS__;
export const isBrowser = (): boolean => !isTauri();
```

所有需要降级的代码路径通过 `isTauri()` 分支，Tauri 路径使用 Tauri FS API，浏览器路径使用 HTTP API。
